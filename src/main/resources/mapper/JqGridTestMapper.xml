<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.fh.mall.dao.JqGridTestMapper">

    <resultMap id="testUser" type="com.fh.mall.entity.TestUser">
        <id column="id" property="id" />
        <result column="name" property="name"/>
        <result column="password" property="password"/>
    </resultMap>

    <select id="getTotalUser" resultType="int">
        select count(1) from mall.tb_user;
    </select>

    <select id="getPageUser" resultMap="testUser">
        select * from mall.tb_user
        order by mall.tb_user.id asc
        limit #{pageParam.start, jdbcType=INTEGER}, #{pageParam.limit, jdbcType=INTEGER}
        #         想要在Mapper中使用，传过来的参数.变量时，必须在传参时用@Param注解标明
#         不使用Map.变量名时，直接使用变量名 e.g.  # {pageParam}，
#         在Mapper的注释中千万不要出现消息表达式符号，Mapper运行的时候注释中的消息表达式也会去正常执行，容易出现没有这个变量名，或者变量不能被设置的错误
    </select>


</mapper>